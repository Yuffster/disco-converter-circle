/**
 * Autogenerated by conversion from a <%= @codebase %> codebase.
 *
 * When using converted areas, please retain original license information and
 * give credit to the original authors.
 * 
 * Generated at: <%= Time.now %>
 */
new Class({

	Extends: Room,

	create: function() {

		this.set_short("<%= @short %>");

		this.set_long("<%= @long %>");

		this.set_type("<%= @type %>");
<% @exits.each do |exit| %>
		this.add_exit("<%=exit['direction']%>", "<%=exit['room']%>");
<% end %>
<% @items.each do |item| %>
		this.add_item("<%=item['keywords']%>", "<%=item['description']%>");
<% end %>
	}

});
